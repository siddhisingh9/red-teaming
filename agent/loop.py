"""Day 3: the multi-turn tool-call loop the agent runs inside."""

from __future__ import annotations

from schemas import RunRecord


def run(*args, **kwargs) -> RunRecord:
    raise NotImplementedError("agent/loop.py is scheduled for day 3")
