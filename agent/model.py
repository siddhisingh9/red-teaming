"""Day 2: generate(messages) -> str, the agent's chat-completion call."""

from __future__ import annotations

from schemas import Turn


def generate(messages: list[Turn]) -> str:
    raise NotImplementedError("agent/model.py is scheduled for day 2")
